"""Engineering-integration primitives (AGENTS.md injection, etc.).

Pure-function building blocks that `super-harness init` and
`super-harness adapter install/uninstall` compose to keep a repo's root
AGENTS.md in sync with the installed framework / agent adapters. See the
engineering-integration spec §2.2 / §3.2.
"""

from __future__ import annotations
